//! flags: -b cpp -o struct
//! commands: clang++ -c main.cpp -o main.o && clang++ main.o foo.o -o main && ./main && rm struct.h && rm struct.o
element Position { x: i32; y: i32; }

func [[export]] add(i: Position, j: Position) -> Position {
    return Position {i.x + j.x, i.y + j.y };
}

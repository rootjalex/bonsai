// Bonsai Header
#pragma once

#include <cstdint>
#include "runtime/bonsai_cpp.h"


extern "C" {
struct Position {
    int32_t x;
    int32_t y = 42;
};

void add(Position& _ret0, const Position& i, const Position& j);
}

------------------------------------------

; LLVM Module
; ModuleID = 'bonsai_module'
source_filename = "bonsai_module"

; Function Attrs: mustprogress nofree norecurse nosync nounwind willreturn memory(argmem: readwrite)
define void @add(ptr nofree noundef nonnull writeonly captures(none) initializes((0, 8)) %_ret0, ptr noundef nonnull readonly captures(none) %i, ptr noundef nonnull readonly captures(none) %j) local_unnamed_addr #0 {
add_entry:
  %0 = load <2 x i32>, ptr %i, align 4
  %1 = load <2 x i32>, ptr %j, align 4
  %2 = add <2 x i32> %1, %0
  store <2 x i32> %2, ptr %_ret0, align 4
  ret void
}

attributes #0 = { mustprogress nofree norecurse nosync nounwind willreturn memory(argmem: readwrite) }

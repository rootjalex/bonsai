// Bonsai Header
#pragma once

#include <cstdint>
#include "runtime/bonsai_cpp.h"


extern "C" {
struct F {
    int32_t x;
    int32_t y;
};
struct G {
    F f;
};

void id(G& _ret0, const G& g);
}

------------------------------------------

; LLVM Module
; ModuleID = 'bonsai_module'
source_filename = "bonsai_module"

; Function Attrs: mustprogress nofree norecurse nosync nounwind willreturn memory(argmem: readwrite)
define void @id(ptr nocapture noundef nonnull writeonly %_ret0, ptr nocapture noundef nonnull readonly %g) local_unnamed_addr #0 {
id_entry:
  %0 = load <2 x i32>, ptr %g, align 4
  store <2 x i32> %0, ptr %_ret0, align 4
  ret void
}

attributes #0 = { mustprogress nofree norecurse nosync nounwind willreturn memory(argmem: readwrite) }

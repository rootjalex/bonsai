// Bonsai Header
#pragma once

#include <array>
#include <cstdint>

extern "C" {

int32_t add(int32_t i, int32_t j);
}

------------------------------------------

; LLVM Module
; ModuleID = 'bonsai_module'
source_filename = "bonsai_module"
target datalayout = "e-m:o-i64:64-i128:128-n32:64-S128-Fn32"
target triple = "arm64-apple-darwin22.6.0"

define i32 @add(i32 noundef %i, i32 noundef %j) {
add_entry:
  %0 = add i32 %i, %j
  ret i32 %0
}

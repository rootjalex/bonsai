#include "runtime/CUDA/helpers.h"

#include <cuda/std/array>
#include <thrust/universal_vector.h>
#include <cuda/std/optional>
#include <cuda/std/tuple>

#include <stdint.h>
#include <variant>

inline __managed__ int32_t counter = 0;
__host__ void foo() {
  atomicAdd((&counter), 1);
  return;
}

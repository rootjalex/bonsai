type ?lambda0 = struct ?lambda0{ call : Fn(i32) -> i32 }
type ?lambda1 = struct ?lambda1{ call : Fn(i32, i32) -> i32 }
type ?lambda2 = struct ?lambda2{ call : Fn(bool, bool) -> bool }
type ?lambda3 = struct ?lambda3{ call : Fn(bool, bool) -> bool }
type ?lambda4 = struct ?lambda4{ call : Fn(i32) -> i32 }
type ?lambda5 = struct ?lambda5{ call : Fn(i32, i32) -> i32 }
type ?lambda6 = struct ?lambda6{ y : i32; call : Fn(i32) -> Fn(i32) -> i32 }


func foo() -> i32 {
  let a = true in
  let b = false in
  let c = (a || b) || false in
  let d = true || b in
  let L1 = build<?lambda0>().call in
  let L2 = build<?lambda1>().call in
  let L3 = build<?lambda2>().call in
  let L4 = build<?lambda3>().call in
  let L5 = build<?lambda4>().call in
  let L6 = build<?lambda5>().call in
  let L7 = build<?lambda6>(y).call in
  let x = L1((i32)0) in
  let y = L2((i32)1, (i32)2) in
  return L6(x, y)
}

func relu(v : set<i32>) -> set<i32> {
  return filter(|x : i32| (i32)0 < x, v)
}



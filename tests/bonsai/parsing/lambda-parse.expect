type ?lambda0 = struct ?lambda0{
  func call(x : i32) -> i32 {
    return x + (i32)1
  }
}
type ?lambda1 = struct ?lambda1{
  func call(x : i32, y : i32) -> i32 {
    return (x + (i32)1) + y
  }
}
type ?lambda2 = struct ?lambda2{
  func call(e : bool, f : bool) -> bool {
    return e || f
  }
}
type ?lambda3 = struct ?lambda3{
  func call(e : bool, f : bool) -> bool {
    return e || f
  }
}
type ?lambda4 = struct ?lambda4{
  func call(y : i32) -> i32 {
    return y
  }
}
type ?lambda5 = struct ?lambda5{
  func call(x : i32, y : i32) -> i32 {
    return x + y
  }
}
type ?lambda6 = struct ?lambda6{
  func call(y : i32) -> i32 {
    return y
  }
}
type ?lambda7 = struct ?lambda7{
  y : i32;
  func call(x : i32) -> Fn(i32) -> i32 {
    return build<?lambda6>().call
  }
}


func foo() -> i32 {
  let a = true in
  let b = false in
  let c = (a || b) || false in
  let d = true || b in
  let L1 = build<?lambda0>().call in
  let L2 = build<?lambda1>().call in
  let L3 = build<?lambda2>().call in
  let L4 = build<?lambda3>().call in
  let L5 = build<?lambda4>().call in
  let L6 = build<?lambda5>().call in
  let L7 = build<?lambda7>(y).call in
  let x = L1((i32)0) in
  let y = L2((i32)1, (i32)2) in
  return L6(x, y)
}

func relu(v : set<i32>) -> set<i32> {
  return filter(|x : i32| (i32)0 < x, v)
}



---CODE---
1
---STDERR---
[internal] Error: src/Lower/ADTs.cpp:633
`layout Shape` stores Sph behind a pointer (`tagged_ptr`), so constructing one allocates it -- which --no-heap forbids. Pick `tagged_index`, which indexes arrays the caller owns, or drop --no-heap.

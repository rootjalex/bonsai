type E = struct E{ x : i32; y : i32 }

func bar(x : i32, y : i32) -> i32 {
  return x + y
}

func foo() -> i32 {
  let a = 1 in
  let b = 2 in
  let c = a + b in
  let d = c in
  return 0
}

func id_no_side_effect(x : i32) -> i32 {
  return x
}

func id_with_side_effect(x : i32) -> i32 {
  print(x)
  return x
}

func if_condition(a : bool, b : bool, c : bool) -> bool {
  r := false
  let c1 = (a && b) && c in
  if (c1) {
    r = a
    let no_cse = (a || b) || c in
  } else {
    let c2 = c1 in
    r = b
    let no_cse = (a || b) || c in
  }
  let c3 = c1 in
  let c4 = (a || b) || c in
  return r
}

func if_statement(x : i32, y : i32, cond : bool) -> void {
  if (cond) {
    let a = x + y in
    let b = a in
  } else {
    let c = x + y in
  }
  let d = x + y in
}

func mutable_struct(e : mut E) -> void {
  e.x += 1
  e.y = 42
}

func mutating_argument(x : mut i32) -> i32 {
  let a = 1 in
  let b = 2 in
  let c = a + x in
  x += 1
  let d = a + x in
  return 0
}

func mutating_value() -> i32 {
  let a = 1 in
  let b = 2 in
  x := 3
  let c = a + x in
  let d = c in
  return 0
}

func nested_binary_operations(x : i32, y : i32) -> void {
  let a = x + y in
  let b = a + 1 in
}

func self_assign() -> void {
  i := 1
  i = i
}

func side_effects() -> i32 {
  let a = 1 in
  let b = id_with_side_effect(a) in
  let c = id_with_side_effect(a) in
  let d = id_no_side_effect(a) in
  let e = d in
  return 0
}

func vector_access(x : i32x3) -> i32 {
  let b_0 = x[0] in
  let b_1 = b_0 in
  return 0
}



func process1(n : i32, a : array[f32, n]) -> array[f32, n] {
    return map(|x : f32| sqrt(x), a);
}

func process2(n : i32, a : array[f32, n]) -> array[f32, n] {
    return map(|x : f32| sqrt(x), a);
}

schedule {
    process1.split(i0, io, ii, 16);
    process2.split(i0, io, ii, 7, i_tail);
}

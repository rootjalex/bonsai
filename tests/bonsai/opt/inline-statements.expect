type Hit = struct Hit{ t : f32; ok : bool }

func clamp_pos(x : f32) -> f32 {
  if (x < 0f) {
    return 0f
  }
  let y = x * 2f in
  return y + 1f
}

func first_big(a : f32[4]) -> f32 {
  forall i in [0:4:1] {
    if (2f < a[i]) {
      return a[i]
    }
  }
  return 0f
}

func half(x : f32) -> f32 {
  if (0f < x) {
    if (10f < x) {
      return 10f
    }
    print(x)
  }
  return x * 0.5f
}

func hit_t(h : Hit) -> f32 {
  if (h.ok) {
    return h.t
  }
  return 1e+30f
}

func note(x : f32) -> void {
  if (1f < x) {
    print(x)
    return
  }
  print(0f - x)
}

func pick(c : bool, a : f32, b : f32) -> f32 {
  if (c) {
    return a
  } else {
    return b
  }
}

func use(x : f32, h : Hit, a : f32[4]) -> f32 {
  clamp_pos$r0 : mut f32
  if (x < 0f) {
    clamp_pos$r0 = 0f
  } else {
    let y$i0 = x * 2f in
    clamp_pos$r0 = y$i0 + 1f
  }
  let p = clamp_pos$r0 in
  hit_t$r1 : mut f32
  if (h.ok) {
    hit_t$r1 = h.t
  } else {
    hit_t$r1 = 1e+30f
  }
  if (hit_t$r1 < p) {
    if (1f < p) {
      print(p)
    } else {
      print(0f - p)
    }
  }
  let x$i3 = x + 1f in
  clamp_pos$r3 : mut f32
  if (x$i3 < 0f) {
    clamp_pos$r3 = 0f
  } else {
    let y$i3 = x$i3 * 2f in
    clamp_pos$r3 = y$i3 + 1f
  }
  hit_t$r4 : mut f32
  if (h.ok) {
    hit_t$r4 = h.t
  } else {
    hit_t$r4 = 1e+30f
  }
  let c$i5 = h.ok in
  pick$r5 : mut f32
  if (c$i5) {
    pick$r5 = clamp_pos$r3
  } else {
    pick$r5 = hit_t$r4
  }
  let q = pick$r5 in
  let r = select(h.ok, clamp_pos(q), q) in
  s : mut f32 := 0f
  if (h.ok && (0f < hit_t(h))) {
    s = 1f
  }
  n : mut i32 := 0
  while (clamp_pos(cast<f32>(n)) < 5f) {
    n = n + 1
  }
  return (((((p + q) + r) + s) + cast<f32>(n)) + first_big(a)) + half(x)
}



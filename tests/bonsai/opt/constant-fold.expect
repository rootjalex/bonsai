func bar(y : i32) -> i32 {
  return 0
}

func baz(z : i32) -> i32 {
  return z
}

func fizz(x : i32, y : i32, c1 : bool, c2 : bool) -> i32 {
  let a = x << 5 in
  let b = y * 31 in
  let c = x & 31 in
  let d = y % 33 in
  let negative1 = -1 in
  let e = x in
  let f = c1 in
  let g = c2 in
  let h = false in
  let i = true in
  return (c + d) + e
}

func foo(x : i32) -> i32 {
  return x
}



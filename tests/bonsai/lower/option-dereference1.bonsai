func foo(i : option[i32], j: option[i32]) -> i32 {
    if i && j {
        return deref(i) + deref(j);
    } else if i {
        return deref(i);
    } else if j {
        return deref(j);
    } else {
        return 0;
    }
}

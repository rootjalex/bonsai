//! flags: -p canonicalize -p lower-trees

element Item {
    id : i32;
}

extern items : set[Item];

func q() = minimum(|x| x.id, items);

schedule {
    tree[[Item]] ItemTree(idl idh : i32) with id in [idl, idh] with min(id) = idl =
    | Interior(left : ItemTree, right : ItemTree)
    | Leaf(data : Item) with data = data;

    items : ItemTree;
}

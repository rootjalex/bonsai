type ?lambda0 = struct ?lambda0{ call : Fn(i32) -> i32 }
type ?lambda1 = struct ?lambda1{ call : Fn(i32) -> i32 }


func foo() -> i32 {
  let L1 = build<?lambda0>().call in
  let y = L1((i32)0) in
  return y
}

func main() -> i32 {
  let L1 = build<?lambda1>().call in
  let y = L1((i32)0) in
  return y
}

func relu(v : set<i32>) -> set<i32> {
  return filter(|x : i32| (i32)0 < x, v)
}



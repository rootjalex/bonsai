
func foo(a : f32, b : f32) {
    if (a < b) {
        print("a is less than b");
    } else {
        print("a is geq than b");
    }
    return;
}

func main() -> i32 {
    foo(2.0, 3.0);
    foo(3.0, 2.0);
    foo(2.0, 2.0);
    return 0;
}